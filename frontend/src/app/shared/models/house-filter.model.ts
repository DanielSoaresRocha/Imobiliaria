export class HouseFilter {
  minValue: number;
  maxValue: number;
  minMeters: number;
  maxMeters: number;
  qtdBedrooms: number;
  qtdBathrooms: number;
  qtdGarage: number;

  constructor() {
    
  }
}